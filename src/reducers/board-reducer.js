export default (
  state = {
    squares: [null, null, null, null, null, null, null, null, null],
    xIsNext: true
  }, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
}
